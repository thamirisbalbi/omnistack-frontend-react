import React from 'react';

export default function Header() { //export default exporta o componente 
    return (
        <header>
            <h1>
                Be The Hero
            </h1>
        </header>

    );
}

